name: Deploy to GKE

on:
  push:
    branches:
      - '**'

env:
  PROJECT_ID: ${{ secrets.GKE_PROJECT }}
  GKE_CLUSTER: dwk-cluster
  GKE_ZONE: europe-north1-b
  REGISTRY: europe-north1-docker.pkg.dev
  REPOSITORY: dwk-docker-repository
  BRANCH: ${{ github.ref_name }}

jobs:
  build-publish-deploy:
    name: Build, Publish and Deploy
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: the-project
    env:
      FORCE_BUILD: ${{ vars.FORCE_BUILD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 2

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GKE_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Google Artifact Registry
        run: gcloud --quiet auth configure-docker europe-north1-docker.pkg.dev

      - name: Get GKE credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: ${{ env.GKE_CLUSTER }}
          project_id: ${{ env.PROJECT_ID }}
          location: ${{ env.GKE_ZONE }}

      - name: Initialize built-images file
        run: |
          rm -f .built_images || true
          touch .built_images

      - name: Build and push todo-app
        run: |
          if [ "${FORCE_BUILD}" = "true" ] || ! git diff --quiet HEAD~1 HEAD -- todo-app/; then
            docker build --tag $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-app:$BRANCH-$GITHUB_SHA todo-app
            docker push $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-app:$BRANCH-$GITHUB_SHA
            echo "PROJECT/todo-app=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-app:$BRANCH-$GITHUB_SHA" >> .built_images
          else
            echo "todo-app unchanged, skipping build"
          fi

      - name: Build and push todo-app-writer
        run: |
          if [ "${FORCE_BUILD}" = "true" ] || ! git diff --quiet HEAD~1 HEAD -- todo-app/picsum-writer/; then
            docker build --tag $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-app-writer:$BRANCH-$GITHUB_SHA todo-app/picsum-writer
            docker push $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-app-writer:$BRANCH-$GITHUB_SHA
            echo "PROJECT/todo-app-writer=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-app-writer:$BRANCH-$GITHUB_SHA" >> .built_images
          else
            echo "todo-app-writer unchanged, skipping build"
          fi

      - name: Build and push todo-backend
        run: |
          if [ "${FORCE_BUILD}" = "true" ] || ! git diff --quiet HEAD~1 HEAD -- todo-backend/; then
            docker build --tag $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-backend:$BRANCH-$GITHUB_SHA todo-backend
            docker push $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-backend:$BRANCH-$GITHUB_SHA
            echo "PROJECT/todo-backend=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-backend:$BRANCH-$GITHUB_SHA" >> .built_images
          else
            echo "todo-backend unchanged, skipping build"
          fi

      - name: Build and push todo-broadcaster
        run: |
          if [ "${FORCE_BUILD}" = "true" ] || ! git diff --quiet HEAD~1 HEAD -- todo-broadcaster/; then
            docker build --tag $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-broadcaster:$BRANCH-$GITHUB_SHA todo-broadcaster
            docker push $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-broadcaster:$BRANCH-$GITHUB_SHA
            echo "PROJECT/todo-broadcaster=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-broadcaster:$BRANCH-$GITHUB_SHA" >> .built_images
          else
            echo "todo-broadcaster unchanged, skipping build"
          fi

      - name: Build and push todo-cronjob
        run: |
          if [ "${FORCE_BUILD}" = "true" ] || ! git diff --quiet HEAD~1 HEAD -- todo-cronjob/; then
            docker build --tag $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-cronjob:$BRANCH-$GITHUB_SHA todo-cronjob
            docker push $REGISTRY/$PROJECT_ID/$REPOSITORY/todo-cronjob:$BRANCH-$GITHUB_SHA
            echo "PROJECT/todo-cronjob=$REGISTRY/$PROJECT_ID/$REPOSITORY/todo-cronjob:$BRANCH-$GITHUB_SHA" >> .built_images
          else
            echo "todo-cronjob unchanged, skipping build"
          fi

      - name: Build and push tododb-backup
        run: |
          if [ "${FORCE_BUILD}" = "true" ] || ! git diff --quiet HEAD~1 HEAD -- tododb-backup/; then
            docker build --tag $REGISTRY/$PROJECT_ID/$REPOSITORY/tododb-backup:$BRANCH-$GITHUB_SHA tododb-backup
            docker push $REGISTRY/$PROJECT_ID/$REPOSITORY/tododb-backup:$BRANCH-$GITHUB_SHA
            echo "PROJECT/tododb-backup=$REGISTRY/$PROJECT_ID/$REPOSITORY/tododb-backup:$BRANCH-$GITHUB_SHA" >> .built_images
          else
            echo "tododb-backup unchanged, skipping build"
          fi

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v2.1.0

      - name: Deploy
        run: |
          NAMESPACE=${GITHUB_REF#refs/heads/}
          [ "$NAMESPACE" = "main" ] && NAMESPACE="project"
          kubectl create namespace $NAMESPACE || true
          # Apply images that were built in this run
          if [ -s .built_images ]; then
            while read -r line; do
              echo "Setting image: $line"
              kustomize edit set image "$line"
            done < .built_images
          else
            echo "No images were built; attempting to preserve currently running images from the cluster"
            # If nothing was built, populate .built_images from images already running in the namespace
            # Requires kubectl access (we have credentials earlier in the job)
            # Read service names from kustomization (images[].name) and query deployments
            if command -v yq >/dev/null 2>&1; then
              SERVICES=$(yq -r '.images[].name' kustomization.yaml | sed 's#PROJECT/##')
            else
              # Fallback: grep image entries as PROJECT/<name> and extract names
              SERVICES=$(grep -E "name: PROJECT/" -A1 the-project/kustomization.yaml 2>/dev/null | sed -n 's/.*PROJECT\///p' || true)
            fi
            for svc in $SERVICES; do
              dep="${svc}-dep"
              img=$(kubectl get deployment "$dep" -n "$NAMESPACE" -o=jsonpath='{.spec.template.spec.containers[0].image}' 2>/dev/null || true)
              if [ -n "$img" ]; then
                echo "PROJECT/$svc=$img" >> .built_images
                echo "Using existing image for $svc -> $img"
              else
                echo "No image found for $svc in namespace $NAMESPACE; skipping"
              fi
            done
          fi

          kustomize edit set namespace $NAMESPACE
          kustomize build . | kubectl apply -f -
          
          # Set NATS_URL based on namespace
          kubectl set env deployment/todo-backend-dep \
            NATS_URL=nats://my-nats.$NAMESPACE.svc.cluster.local:4222 \
            -n $NAMESPACE
          
          kubectl set env deployment/todo-broadcaster-dep \
            NATS_URL=nats://my-nats.$NAMESPACE.svc.cluster.local:4222 \
            -n $NAMESPACE
          
          kubectl rollout status statefulset/todo-app -n $NAMESPACE
          kubectl get services -n $NAMESPACE -o wide